using System;

public class Solution {
    public int solution(int n) {
        int answer = 2;
        for (int i = 0; i<n; i++) 
            if(n == (i+1)*(i+1))
                answer = 1;
        
        return answer;
    }
}
